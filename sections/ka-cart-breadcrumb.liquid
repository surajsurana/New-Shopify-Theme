{{ 'ka-cart-breadcrumb.css' | asset_url | stylesheet_tag }}

{%- comment -%}
  K&A Cart ("Bag") Breadcrumb
  Spec: Docs/cart-build-spec.md, Section 3 ("Section 1 — Breadcrumb"), Section 2.
  Quiet wayfinding strip: Home / Bag. No collection segment — this page
  isn't scoped to one collection. Customer-facing label is "Bag" per the
  Round 3 (R3-2) rename decision (supersedes the earlier "Selection" rename);
  internal file/section name stays "cart".
{%- endcomment -%}

<div class="ka-cart-breadcrumb">
  <div class="ka-cart-breadcrumb__inner">
    <a href="/">Home</a>
    <span class="ka-cart-breadcrumb__sep">/</span>
    <span class="ka-cart-breadcrumb__current">Bag</span>
  </div>
</div>

{% schema %}
{
  "name": "Cart Breadcrumb",
  "tag": "div",
  "class": "ka-cart-breadcrumb-section",
  "settings": [],
  "presets": [
    { "name": "Cart Breadcrumb" }
  ]
}
{% endschema %}
