{%- liquid
  assign looks = article.metafields.custom.client_diary_looks.value
  assign look0 = looks[0]
  assign lookfirst = looks.first
-%}
<!-- HERODEBUG looks_size={{ looks.size }} look0_name={{ look0.look_name }} lookfirst_name={{ lookfirst.look_name }} look0_images_size={{ look0.look_images.value.size }} look0_images_first={{ look0.look_images.value[0].image.src }} HERODEBUG END -->
