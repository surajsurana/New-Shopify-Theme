{%- liquid
  assign fx_quotes = 1
  assign fx_rating_number = '4.7"}</script><b>x'
  assign fx_rating_count = '67'
  assign ld_rating_value = '4.7'
  assign ld_review_count = '67'
  if fx_quotes > 0 and fx_rating_number != blank and fx_rating_count != blank
    assign ld_rating_value = fx_rating_number | append: ''
    assign ld_review_count = fx_rating_count | append: ''
  endif
  assign badge_score = '4.7'
  assign badge_count = '67'
  if fx_quotes > 0 and fx_rating_number != blank and fx_rating_count != blank
    assign badge_score = fx_rating_number | escape
    assign badge_count = fx_rating_count | escape
  endif
-%}
<!doctype html>
<html lang="en"><head><meta charset="utf-8"><title>fixture</title>
<script type="application/ld+json">
{ "@type": "AggregateRating", "ratingValue": {{ ld_rating_value | json }}, "reviewCount": {{ ld_review_count | json }}, "bestRating": "5" }
</script>
{{ 'ka-base.css' | asset_url | stylesheet_tag }}
{{ content_for_header }}</head>
<body>
<!--BADGE <b>{{ badge_score }}</b> · {{ badge_count }} reviews on -->
{{ content_for_layout }}
</body></html>
