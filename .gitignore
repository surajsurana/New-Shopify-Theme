# Claude Code local-only settings (per-machine, not shared)
.claude/settings.local.json

# OS junk
.DS_Store
Thumbs.db

# Shopify CLI local state
.shopify/

# Working state for scripts/get-staging-verification-query.ps1 +
# compare-staging-verification.ps1 (local-only, regenerated each deploy)
.staging-verify/

# Node (in case tooling gets added later)
node_modules/
